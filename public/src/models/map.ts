/**
 * Gives the latitude and longitude of a position on the map.
 */
export type GeoPosition = { latitude: number, longitude: number }

/**
 * The user settings of the map.
 */
export type MapSettings = { centerLocation: GeoPosition }
